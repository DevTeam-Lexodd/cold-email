{
  "name": "cold-email",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently -n api,worker,ui -c blue,yellow,green \"cd backend && npm run dev:api\" \"cd backend && npm run worker\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}